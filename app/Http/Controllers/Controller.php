<?php

declare(strict_types=1);

namespace App\Http\Controllers;

// Base controller class — all application controllers extend this.
// Kept minimal: Laravel 11 no longer ships it by default, so we define it here.
abstract class Controller
{
    //
}
